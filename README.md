# Hello-wasm

Using Wasm to build a node package with rust.

Use the live server to run de index.html on the site folder, there's an example for the library usage.

## The MDN docs for Rust WebAssembly
https://developer.mozilla.org/en-US/docs/WebAssembly/Rust_to_wasm
